export class Project {
  id: string;          // Ej: "project:alpha:erp"
  orgCode: string;     // Ej: "alpha"
  code: string;        // Ej: "erp"
  name: string;        // Ej: "Sistema ERP Interno"
  description: string;
  createdAt: Date;
}
